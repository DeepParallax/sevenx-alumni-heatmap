# SevenX Alumni Heatmap

成都七中校友联络热力图前端。页面从阿里云函数计算接口读取聚合数据，不包含飞书密钥或联系人原始数据。

## Local development

```bash
npm install
npm run dev
```

## Deployment

推送到 `main` 分支后，GitHub Actions 会自动构建并部署到 GitHub Pages。
